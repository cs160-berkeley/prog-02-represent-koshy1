<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:clickable="true">

    <ProgressBar
        android:id="@+id/loading_screen_progress_bar"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:id="@+id/loading_screen_error_container"
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="@dimen/large_title_text_size"
            android:textColor="@color/represent_dark_red"
            android:fontFamily="sans-serif-medium"
            android:text = "@string/loading_screen_error_title" />

        <Button
            android:id="@+id/loading_screen_action_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/common_element_spacing"
            android:backgroundTint="@color/represent_dark_red"
            android:textColor="@android:color/white"
            android:layout_gravity="center"
            android:text="@string/loading_screen_action_button_text" />

    </LinearLayout>

</RelativeLayout>
